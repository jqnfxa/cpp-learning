#pragma once

#include <string>
#include <vector>

/*
 * Step 21 -- brace-or-equal NSDMIs. See task.md for the spec.
 *
 * Declare struct config inside task21 with the members and defaults
 * listed in task.md. Use NSDMIs only; don't write a user-defined
 * default constructor.
 */

namespace task21
{

/* TODO: declare struct config with these members and defaults:
 *   int                       retries     = 3
 *   int                       timeout_ms  = 5000
 *   std::string               name        = "default"
 *   bool                      verbose     = false
 *   std::vector<std::string>  tags        = empty
 */

}
