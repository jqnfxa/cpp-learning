#pragma once

#include <string>
#include <vector>

/*
 * Step 08 -- type aliases with `using`. See task.md for the spec.
 *
 * Declare four aliases inside namespace task08, using the
 * `using NAME = TYPE;` syntax. The tests reference them by name, so
 * if any are missing the test file won't compile.
 *
 *   u64            -> unsigned long long
 *   int_binary_op  -> pointer to int (int, int)
 *   string_vec     -> std::vector<std::string>
 *   vec_int_iter   -> std::vector<int>::iterator
 */

namespace task08
{

/* TODO: declare the four aliases listed above. */

}
