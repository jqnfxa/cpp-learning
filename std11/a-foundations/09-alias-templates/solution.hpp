#pragma once

#include <array>
#include <cstddef>
#include <map>
#include <vector>

/*
 * Step 09 -- alias templates. See task.md for the spec.
 *
 * Declare four alias templates inside namespace task09:
 *
 *   template<class T>                       vec<T>      -> std::vector<T>
 *   template<class K, class V>              map<K, V>   -> std::map<K, V>
 *   template<class T, std::size_t N>        array<T,N>  -> std::array<T, N>
 *   template<class T>                       ptr<T>      -> T *
 *
 * Use the `template <...> using NAME = TYPE;` syntax.
 */

namespace task09
{

/* TODO: declare the four alias templates listed above. */

}
